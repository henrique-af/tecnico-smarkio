{
  "scripts": {
    "install": "cd front && npm install & cd ../back && npm install",
    "site": "cd front && npm start",
    "servidor": "cd back && cd app && npx sequelize db:create && cd .. && node server.js"
  },
  "dependencies": {
    "node": "^16.5.0"
  }
}
